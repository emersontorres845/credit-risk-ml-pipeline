{
  "name": "predictbi-login",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",            
    "build": "vite build",         
    "start": "vite preview --port $PORT --host",
    "test": "jest",
    "lint": "eslint ."
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "vite": "^5.0.0"
  }
}