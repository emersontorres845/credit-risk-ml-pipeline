{
  "name": "credit-risk-ml-pipeline",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "vite preview --port $PORT --host",
    "test": "jest",
    "lint": "eslint ."
  },
  "dependencies": {
    "vite": "^5.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "eslint": "^8.56.0"
  }
}